extends _template
block body
  ul.nav.nav-tabs(role='tablist')
    li.nav-item
      .nav-link.active(id='topup-content' data-toggle="tab" href="#topup" role='tab' aria-control='topup' aria-selected='true') แพ็กเกจเติมเงิน 
    li.nav-item  
      .nav-link(id='monthly-content' data-toggle="tab" href="#monthly" role='tab' aria-control='monthly' aria-selected='false') แพ็กเกจรายเดือน  
  .tab-content
    .tab-pane.fade.show.active(id='topup' role='rabpanel' aria-labelledby='topup-content')
      h1 Hi TopUp
    .tab-pane.fade.show(id='monthly' role='rabpanel' aria-labelledby='monthly-content')
      h1 Hi Monthly